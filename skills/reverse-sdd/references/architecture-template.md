# Template: 01-ARCHITECTURE.md

Golden rule: every non-trivial claim carries `[VERIFY: path/file.ext:line]`.
Anything that cannot be anchored to code is marked `[INFERRED: reason]`.

---

# Architecture — {system name}

> Generated by reverse engineering on {date}. Reference commit: `{HEAD hash}`.
> Purpose: foundation for the v2 rebuild. This is not aspirational documentation:
> it describes what the code IS today.

## 1. Context view

- **Purpose of the system** (1-3 sentences; if the repo doesn't make it obvious, `[INFERRED]`)
- **External actors**: users, systems that consume it, systems it consumes
  (each integration with its evidence: HTTP client, SDK, queue, etc.)
- Context diagram in Mermaid (`C4Context` or a simple `graph TD`)

## 2. Component view

For each main component (module, service, layer):

### 2.x {Component}
- **Responsibility**: what it does and what it does NOT do
- **Location**: `[VERIFY: src/...]`
- **Input/Output**: endpoints, events, public functions
- **Depends on**: internal and external components

Close with a Mermaid diagram of the components and their dependencies.

## 3. Main flows

Pick the 3-7 flows that define the system (the ones the largest user stories implement).
For each one: Mermaid sequence diagram + step-by-step narration with `[VERIFY:]`
at every hop between components.

## 4. Data model (summary)

Main entities, relationships, and where they live (collections/tables), with evidence
in the code's schemas/models. The full detail goes in the SDD Data Model spec;
here only the map.

## 5. Observed architecture decisions

| # | Decision | Evidence | Keep in v2? | Rationale |
|---|---|---|---|---|
| AD-1 | e.g.: Repository pattern over direct access | `[VERIFY:]` | Yes/No/Review | |

## 6. Historical decisions (superseded)

Designs that existed and were replaced, detected via `refactor` clusters in the
history. Format: what existed, what replaced it, `[COMMITS: ...]`, and the lesson for v2
(so dead ends are not re-walked).

## 7. Cross-cutting concerns

- **Authentication/authorization**: mechanism and where it is enforced `[VERIFY:]`
- **Error handling**: dominant pattern (exceptions, Result, middleware) `[VERIFY:]`
- **Logging/observability**: what is logged and with what `[VERIFY:]`
- **Configuration**: how settings/secrets arrive (env, files) `[VERIFY:]`

## 8. Visible technical debt

An honest list: hotspots with recurring fixes (from the history analysis), relevant
TODOs/FIXMEs, dangerous couplings. Each item with evidence. This section feeds
directly into "Lessons for v2" in 03-EVOLUTION.md.
